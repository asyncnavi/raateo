package ui

templ URLButton(title string, href string, color string) {
	<a
		href={ templ.SafeURL(href) }
		class={ "bg-"+color,"font-bold inline-flex items-center  border-2 border-black py-2 px-3 rounded-full shadow-[4px_4px_black] hover:shadow-none " }
	>
		{ title }
	</a>
}
